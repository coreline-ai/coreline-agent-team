# implement_20260403_185856.md

작성 일시: `2026-04-03 18:58:56 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
현재 `agent-team`의 실사용 경로에서 마지막으로 체감되는 운영 불편을 줄이기 위해,
background worker 관측성과 generated preview UX를 개선하고, longer soak / restart
검증을 더 체계적으로 남기는 것이 목적이다.

## 개발 범위
- background teammate의 PID / 실행 상태 / 로그 가시성 개선
- `attach`, `status`, project builder에서 generated files / preview 요약 UX 개선
- `Codex CLI` repeated soak / restart 검증 결과 축적과 문서화

## 제외 범위
- 새로운 runtime/backend 추가
- direct API 기반 LLM 경로 추가
- remote transport / pane backend / multi-machine 확장
- preset/role 체계 재설계
- 무관한 대규모 리팩토링

## 참조 문서
- [README.md](../README.md)
- [AGENT.md](../AGENT.md)
- [DEV_PLAN_WORKFLOW.md](../docs/DEV_PLAN_WORKFLOW.md)
- [GOAL_CLOSURE_PLAN.md](../docs/GOAL_CLOSURE_PLAN.md)
- [CODEX_REPEATED_SOAK.md](../docs/CODEX_REPEATED_SOAK.md)
- [FINAL_USER_MANUAL_CHECKLIST.md](../docs/FINAL_USER_MANUAL_CHECKLIST.md)

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약
- [x] Phase 1. Background Worker Visibility 완료
- [x] Phase 2. Generated Preview UX Polish 완료
- [x] Phase 3. Soak / Restart Verification Hardening 완료

---

## Phase 1. Background Worker Visibility

### 목표
- detached background worker의 PID, 실행 상태, 마지막 상태 전이 정보를 사용자 표면에서 더 명확히 본다.

### 구현 태스크
- [x] background worker metadata 저장 구조를 점검하고 현재 노출 가능한 필드를 정리한다.
- [x] `attach`와 `status`에서 PID / lifecycle summary를 일관되게 노출한다.
- [x] 필요하면 TUI / project builder 상태 패널에 background worker visibility를 추가한다.

### 자체 테스트
- [x] background worker metadata 저장/조회 테스트를 추가한다.
- [x] `attach` / `status` 출력에 visibility 필드가 노출되는지 확인한다.
- [x] `npm run typecheck`를 통과한다.
- [x] `npm test`를 통과한다.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 2. Generated Preview UX Polish

### 목표
- 생성 파일이 많아질 때 `attach`와 project builder의 상단 요약이 덜 잘리고, 핵심 산출물을 더 빨리 파악할 수 있게 한다.

### 구현 태스크
- [x] 현재 generated files / preview 요약 규칙을 분석한다.
- [x] 긴 파일 목록과 preview 우선순위 규칙을 정리한다.
- [x] `attach`와 project builder의 summary surface를 더 읽기 쉬운 형태로 정리한다.

### 자체 테스트
- [x] preview selection / truncation 규칙 테스트를 추가한다.
- [x] generated file이 많은 케이스에서 attach/app 출력이 읽기 쉬운지 smoke로 확인한다.
- [x] `npm run typecheck`를 통과한다.
- [x] `npm test`를 통과한다.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 3. Soak / Restart Verification Hardening

### 목표
- repeated soak와 restart validation을 더 체계적으로 남겨서 운영 신뢰도를 높인다.

### 구현 태스크
- [x] 현재 `soak:codex` 경로와 문서를 기준으로 반복 검증 포인트를 재정리한다.
- [x] restart / reopen / attach 재진입 케이스를 문서와 smoke 시나리오에 반영한다.
- [x] burn-in 결과를 남기는 최소 운영 규칙을 정리한다.

### 자체 테스트
- [x] repeated soak 관련 자동 테스트 또는 smoke 절차를 보강한다.
- [x] restart / reopen / attach 관련 문서 기반 smoke를 갱신한다.
- [x] `npm run typecheck`를 통과한다.
- [x] `npm test`를 통과한다.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능
