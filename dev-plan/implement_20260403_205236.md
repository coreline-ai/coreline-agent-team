# implement_20260403_205236.md

작성 일시: `2026-04-03 20:52:36 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
background worker의 stdout/stderr를 저장하고 attach/status/TUI에서 최근 로그를 확인할 수 있게 해 운영 디버깅 가시성을 높인다.

## 개발 범위
background process logging, log path persistence, attach/status/TUI visibility, 관련 테스트

## 제외 범위
- 새 runtime/backend 추가
- multi-team UX 확장
- exact upstream parity

## 참조 문서
- README.md
- AGENT.md
- docs/DEV_PLAN_WORKFLOW.md
- dev-plan/implement_20260403_204830.md

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약
- [x] Phase 1. Worker log capture 완료
- [x] Phase 2. Log visibility surfaces 완료

---

## Phase 1. Worker log capture

### 목표
- detached background worker의 stdout/stderr를 파일로 남기고, 런타임 상태에 log path를 연결한다.

### 구현 태스크
- [x] `src/team-core/paths.ts`에 worker log 디렉터리와 stdout/stderr log path helper를 추가한다.
- [x] `src/team-core/file-utils.ts`에 text append와 tail read 유틸을 추가한다.
- [x] `src/team-core/types.ts`, `src/team-runtime/types.ts`에 log path metadata를 반영한다.
- [x] `src/team-operator/background-process.ts`에서 detached spawn 시 stdout/stderr를 로그 파일로 리다이렉트한다.
- [x] `src/team-core/task-store.ts`가 detached worker의 log path와 tail을 읽어 `AgentStatus`에 넣도록 확장한다.

### 자체 테스트
- [x] `tests/team-operator/background-process.test.ts`에서 detached spawn이 log file과 fd 기반 stdio를 구성하는지 검증한다.
- [x] `tests/team-core/task-store.test.ts`, `tests/team-runtime/spawn-in-process.test.ts`에서 launch visibility metadata가 상태로 노출되는지 검증한다.
- [x] `npm run typecheck`를 실행한다.
- [x] `npm test`를 실행한다.

### 이슈 및 수정
- [x] 테스트가 log 디렉터리를 만들지 않고 직접 파일을 써서 `ENOENT`가 발생했고, 테스트에서 parent directory를 먼저 생성하도록 수정했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 2. Log visibility surfaces

### 목표
- attach/status/TUI에서 recent stderr signal과 log path를 확인할 수 있게 한다.

### 구현 태스크
- [x] `src/team-cli/commands/status.ts`에 `stdout_log`, `stderr_log`, `stderr_tail` 출력을 추가한다.
- [x] `src/team-cli/commands/attach.ts`에 teammate log summary를 추가한다.
- [x] `src/team-tui/components/teammates-pane.tsx`에 detached worker의 log path와 latest stderr preview를 표시한다.
- [x] TUI teammate preview가 좁은 폭에서 잘리지 않도록 focus-mode 경로 기준으로 검증한다.

### 자체 테스트
- [x] `tests/team-cli/attach-command.test.ts`에 stdout/stderr log summary 검증을 추가한다.
- [x] `tests/team-cli/resume-cleanup.test.ts`에 status output의 stderr path/tail 검증을 추가한다.
- [x] `tests/team-tui/app.test.tsx`에 detached teammate stderr preview 렌더 검증을 추가한다.
- [x] `npm run build && node --test dist/tests/team-tui/app.test.js`로 TUI 단독 회귀를 확인한다.
- [x] `npm test`를 다시 실행해 전체 회귀를 확인한다.

### 이슈 및 수정
- [x] teammate 상태 기대값을 `busy`로 둔 테스트가 실제 상태 계산(`idle`)과 달라 실패했고, detached + task 미할당 케이스에 맞게 테스트를 보정했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능
