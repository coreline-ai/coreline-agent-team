# implement_20260403_204830.md

작성 일시: `2026-04-03 20:48:30 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
기본 workspace 경로를 repo 밖으로 옮기고 문서 상태를 최신 구현 기준으로 동기화한다.

## 개발 범위
run/app의 기본 workspace 정책, 관련 테스트, README/AGENT 상태 sync

## 제외 범위
- 새 runtime/backend 추가
- worker log tail 구현
- multi-team UX 확장

## 참조 문서
- README.md
- AGENT.md
- docs/DEV_PLAN_WORKFLOW.md
- dev-plan/implement_20260403_201710.md

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약
- [x] Phase 1. Workspace path realignment 완료
- [x] Phase 2. Documentation sync 완료

---

## Phase 1. Workspace path realignment

### 목표
- `run`과 `app`이 결과물을 기본적으로 repo 바깥 workspace에 만들도록 정책을 통일하고,
  경로가 다시 `process.cwd()/agent-team-output/...`로 회귀하지 않게 테스트를 고정한다.

### 구현 태스크
- [x] 기본 workspace 경로 정책을 `TeamCoreOptions.rootDir/workspaces/<team>`로 중앙화한다.
- [x] `run` 명령이 새 기본 workspace helper를 사용하도록 변경한다.
- [x] `ProjectStudioApp`이 같은 기본 workspace helper를 재사용하도록 변경한다.
- [x] 경로 helper 테스트와 `run` 기본 workspace 통합 테스트를 추가한다.

### 자체 테스트
- [x] `getDefaultWorkspacePath()` 경로 helper 테스트를 추가한다.
- [x] `run`이 workspace 미지정 시 `<root-dir>/workspaces/<team>`에 산출물을 쓰는지 테스트한다.
- [x] `npm run typecheck`를 통과한다.
- [x] `npm run build && node --test dist/tests/team-core/paths.test.js dist/tests/team-cli/run-command.test.js`를 통과한다.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 2. Documentation sync

### 목표
- README와 AGENT handoff 문서가 최신 workspace 정책과 최신 테스트 상태를 반영하도록 동기화한다.

### 구현 태스크
- [x] README에 기본 workspace 정책과 기본 경로 예시를 반영한다.
- [x] AGENT handoff에 기본 workspace 정책과 최신 테스트 상태를 반영한다.
- [x] 오래된 테스트 수/상태 문구를 최신 값으로 정리한다.

### 자체 테스트
- [x] 문서가 최신 구현 상태와 모순되지 않는지 수동 점검한다.
- [x] `npm test` 전체 회귀 결과를 기준으로 테스트 수를 최신화한다.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 이번 plan 범위 완료
