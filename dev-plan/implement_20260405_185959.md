# implement_20260405_185959.md

작성 일시: `2026-04-05 18:59:59 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
long-lived burn-in / release checklist 운영 기준을 고정한다.

## 개발 범위
real backend repeated soak 운영 절차, release gate, artifact 판독 기준 문서화에 한정한다.

## 제외 범위
- 새 runtime/backend 추가
- direct API 기반 LLM 경로 추가
- multi-team overview 구현
- approval / permission UX 재설계

## 참조 문서
- 이전 개발 계획: dev-plan/implement_20260405_181634.md
- README.md
- AGENT.md
- docs/TEAM_CONSTRAINTS.md
- docs/RELIABILITY_CHECKLIST.md
- docs/CODEX_REPEATED_SOAK.md
- docs/NEXT_BACKLOG.md
- docs/DEV_PLAN_WORKFLOW.md

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- release gate는 `Codex CLI` 경로 우선으로 정의하고, upstream `claude` backend는 보조 검증으로만 다룬다.
- soak PASS/FAIL 판정은 콘솔 문자열보다 artifact (`latest-summary.json`, `failure-*.json`)를 우선한다.

## Phase 상태 요약
- [x] Phase 1. burn-in runbook / release checklist baseline 완료
- [x] Phase 2. soak gate / artifact 판독 기준 강화 완료
- [x] Phase 3. 문서 / 운영 절차 릴리즈 동기화 완료

---

## Phase 1. burn-in runbook / release checklist baseline

### 목표
- release 전 최소 검증 순서를 한 문서로 고정한다.
- `5 / 10 iteration` repeated soak 실행 커맨드와 PASS/FAIL 판독 기준을 재사용 가능한 runbook으로 정리한다.

### 구현 태스크
- [x] `docs/RELEASE_CHECKLIST.md`를 새로 만들고 release gate 원칙을 정리한다.
- [x] 변경 유형별 최소 검증 기준(`docs-only`, `UX`, `runtime/session/task`, `bridge`)을 표로 고정한다.
- [x] `5 / 10 iteration` 실행 커맨드, artifact 판독 순서, blocker failure pattern을 checklist에 정리한다.
- [x] `docs/CODEX_REPEATED_SOAK.md`, `docs/RELIABILITY_CHECKLIST.md`에서 새 checklist를 참조하도록 연결한다.

### 자체 테스트
- [x] 새 checklist에 적힌 soak command / artifact path / JSON field 이름이 기존 `docs/CODEX_REPEATED_SOAK.md`와 일치하는지 수동 대조한다.
- [x] `docs/CODEX_REPEATED_SOAK.md`, `docs/RELIABILITY_CHECKLIST.md` 링크가 새 문서를 참조하는지 확인한다.
- [x] 문서 범위가 `Codex CLI` 경로 우선 원칙과 현재 backlog 우선순위를 벗어나지 않는지 점검한다.

### 이슈 및 수정
- [x] 기존 soak 문서만으로는 “언제 5 iteration, 언제 10 iteration을 요구하는지”가 분산되어 있어, 변경 유형별 release gate 표를 별도 checklist로 분리했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 2. soak gate / artifact 판독 기준 강화

### 목표
- repeated soak 결과를 더 빠르게 release decision으로 연결할 수 있게 summary / 판독 surface를 정리한다.

### 구현 태스크
- [x] soak 결과에서 release blocker를 빠르게 읽는 helper surface 또는 문서 패턴을 보강한다.
- [x] `latest-summary.json` 기준 PASS/FAIL gate를 더 기계적으로 재사용할 수 있는 방법을 정한다.
- [x] `scripts/check-codex-repeated-soak.mjs`와 `npm run soak:codex:check`를 추가하고, 기존 soak runner에도 `--gate` 판정을 연결한다.

### 자체 테스트
- [x] `tests/team-cli/codex-repeated-soak.test.ts`에 release gate evaluation / checker script 테스트를 추가한다.
- [x] `npm run typecheck`
- [x] `npm test` — 총 181 tests 통과

### 이슈 및 수정
- [x] checklist 표만으로는 PASS/FAIL을 사람이 매번 다시 해석해야 해서, named gate(`permission/runtime/bridge`)와 exit code 기반 checker를 추가했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 3. 문서 / 운영 절차 릴리즈 동기화

### 목표
- README / AGENT / backlog 문서가 새 burn-in / release checklist와 같은 운영 기준을 설명하도록 맞춘다.

### 구현 태스크
- [x] README에 release 전 repeated soak와 checklist 참조를 반영한다.
- [x] AGENT / NEXT_BACKLOG / DEV_PLAN_WORKFLOW를 최신 우선순위와 함께 동기화한다.
- [x] CLAUDE와 관련 문서의 테스트 수를 최신 상태로 갱신한다.

### 자체 테스트
- [x] 문서에 적힌 명령 / 파일 경로 / 체크 기준이 실제 저장소와 일치하는지 수동 점검한다.
- [x] Phase 2 직후 확인한 `npm run typecheck` / `npm test` 결과(`181 tests pass`)를 문서 수치에 반영한다.
- [x] 새 next-work priority가 backlog / workflow / handoff 문서에서 서로 모순되지 않는지 확인한다.

### 이슈 및 수정
- [x] burn-in checklist가 완료되면서 다음 우선순위가 `multi-team picker / overview`로 이동했기 때문에, backlog / workflow / handoff의 추천 workstream을 함께 갱신했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## 최종 결과 요약

- `docs/RELEASE_CHECKLIST.md`와 `npm run soak:codex:check` 기준의 운영 release gate가 문서 전반에 반영됐다.
- README / AGENT / CLAUDE / backlog / workflow가 같은 burn-in 모델과 같은 다음 우선순위를 설명한다.
- 최신 로컬 검증 수치는 `181 tests pass`로 동기화됐다.
