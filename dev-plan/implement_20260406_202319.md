# implement_20260406_202319.md

작성 일시: `2026-04-06 20:23:19 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
richer global dashboard / global ops surface를 정의하고 구현 범위를 고정한다.

## 개발 범위
전역 운영 overview, cross-team attention/approval/stale surface, global dashboard 진입 UX에 한정한다.

## 제외 범위
- 새 runtime/backend 추가
- direct API 기반 LLM 경로 추가
- permission persistence 규칙 재설계
- remote transport / pane backend 추가

## 참조 문서
- README.md
- AGENT.md
- docs/TEAM_CONSTRAINTS.md
- docs/NEXT_BACKLOG.md
- docs/DEV_PLAN_WORKFLOW.md
- dev-plan/implement_20260405_201816.md
- dev-plan/implement_20260405_205926.md

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 기존 `attach/watch/tui`의 단일 팀 상세 흐름은 유지하고, 이번 workstream은 여러 팀을 가로지르는 전역 운영 surface만 다룬다.

## Phase 상태 요약
- [x] Phase 1. global overview data contract 완료
- [x] Phase 2. global ops surface 완료
- [x] Phase 3. 문서 / smoke / regression sync 완료

---

## Phase 1. global overview data contract

### 목표
- team picker 수준의 목록 요약을 넘어서, 여러 팀 전체를 한 번에 판독할 수 있는 전역 운영 summary contract를 만든다.
- attention team, pending approval, stale worker, active worker, blocked task를 root-dir 단위로 집계할 수 있게 한다.

### 구현 태스크
- [x] `team-operator` 계층에 root-dir 기준 global dashboard model을 추가한다.
- [x] 팀별 overview를 재사용해 `attentionTeams`, `pendingApprovalsTotal`, `activeWorkersTotal`, `staleWorkersTotal`, `blockedOrPendingTeams` 같은 전역 집계를 정의한다.
- [x] approval pending / stale worker / attention reason을 cross-team 카드나 섹션에 바로 쓸 수 있는 최소 데이터 구조로 정리한다.
- [x] empty root / single-team / multi-team / all-completed 상태에서 결과가 흔들리지 않도록 정리한다.

### 자체 테스트
- [x] `tests/team-operator/dashboard.test.ts`에 global summary total / sorting / attention grouping 검증을 추가한다.
- [x] `npm run typecheck`
- [x] `npm test`
- [x] 결과를 확인한다.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 2. global ops surface

### 목표
- 전역 운영자가 “어느 팀을 먼저 봐야 하는가”를 한 화면에서 판단할 수 있게 한다.
- team picker를 단순 목록에서 global ops entrypoint 수준으로 끌어올린다.

### 구현 태스크
- [x] `src/team-tui/app.tsx` 또는 관련 컴포넌트에 global overview panel/section을 추가한다.
- [x] 전역 화면에서 최소한 아래를 바로 볼 수 있게 한다.
  - [x] attention-needed 팀 목록
  - [x] pending approval이 있는 팀 목록
  - [x] stale worker / active worker 상위 팀
  - [x] pending task가 많은 팀 또는 block된 팀
- [x] 전역 화면에서 선택한 팀으로 바로 진입하는 흐름을 정리한다.
- [x] 필요 시 `watch` 또는 읽기 전용 global summary surface까지 최소 범위로 확장할지 판단하고, 문서에 없는 확장은 하지 않는다.

### 자체 테스트
- [x] `tests/team-tui/app.test.tsx`에 global overview render / team jump / empty-state 회귀 테스트를 추가한다.
- [x] 필요 시 operator 계층 액션/selector 테스트를 보강한다.
- [x] `npm run typecheck`
- [x] `npm test`
- [x] 결과를 확인한다.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 3. 문서 / smoke / regression sync

### 목표
- 전역 운영 surface의 사용법과 한계를 README / AGENT / smoke 문서에 맞춘다.
- multi-team overview와 global ops surface의 차이를 문서에서 명확히 구분한다.

### 구현 태스크
- [x] `README.md`, `AGENT.md`에 global ops surface와 진입 경로를 반영한다.
- [x] `docs/TUI_SMOKE.md`, 필요 시 `docs/CLI_SMOKE.md`에 global overview 확인 절차를 추가한다.
- [x] `docs/NEXT_BACKLOG.md`, `docs/DEV_PLAN_WORKFLOW.md`의 다음 우선순위를 최신 상태로 동기화한다.
- [x] 최신 테스트 수와 완료된 dev-plan 이력을 함께 반영한다.

### 자체 테스트
- [x] 문서에 적힌 화면 이름 / state / shortcut이 실제 구현과 맞는지 수동 점검한다.
- [x] Phase 2 직후 확인한 `npm run typecheck` / `npm test` 결과를 문서 수치에 반영한다.
- [x] backlog / workflow / handoff의 다음 우선순위가 서로 모순되지 않는지 확인한다.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능
