# implement_20260405_201816.md

작성 일시: `2026-04-05 20:18:16 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
multi-team picker / overview를 운영 대시보드 수준으로 개선한다.

## 개발 범위
team list overview, attention-needed team 식별, team picker summary surface에 한정한다.

## 제외 범위
- 새 runtime/backend 추가
- direct API 기반 LLM 경로 추가
- permission / approval persistence 재설계
- generated files / preview large-output UX 전면 개편

## 참조 문서
- 이전 개발 계획: dev-plan/implement_20260405_185959.md
- README.md
- AGENT.md
- docs/TEAM_CONSTRAINTS.md
- docs/NEXT_BACKLOG.md
- docs/DEV_PLAN_WORKFLOW.md
- docs/RELIABILITY_CHECKLIST.md

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 단일 팀 상세 화면(`attach/watch/tui`)은 유지하고, 이번 workstream은 multi-team 선택/요약 surface만 다룬다.

## Phase 상태 요약
- [x] Phase 1. multi-team overview data contract 완료
- [x] Phase 2. team picker UI / attention surface 완료
- [x] Phase 3. 문서 / smoke / regression sync 완료

---

## Phase 1. multi-team overview data contract

### 목표
- 각 팀을 “이름만 있는 목록”이 아니라 운영 판단 가능한 summary item으로 읽을 수 있게 한다.
- approval pending, active worker, pending task, stale worker를 팀 목록 계약에 포함한다.

### 구현 태스크
- [x] `TeamListItem`에 result state / approval count / worker count / task count / attention reason 필드를 추가한다.
- [x] `listTeams()`가 각 팀별 `tasks`, `approvals`, `statuses`, unread leader mailbox를 집계하도록 확장한다.
- [x] attention / running / pending / completed 우선순위로 team list를 정렬한다.

### 자체 테스트
- [x] `tests/team-operator/dashboard.test.ts`에 multi-team overview count / sorting 검증을 추가한다.
- [x] `npm run typecheck`
- [x] `npm test` — 총 183 tests 통과

### 이슈 및 수정
- [x] `getAgentStatuses()`는 `team-lead`도 포함하므로, overview worker count는 teammate만 대상으로 계산하도록 분리했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 2. team picker UI / attention surface

### 목표
- 팀 선택 화면만 봐도 어떤 팀이 attention 상태인지 바로 보이게 한다.
- team picker가 항상 “새 팀 생성”부터 시작하지 않고, 기존 팀이 있으면 overview list를 먼저 보여주게 만든다.

### 구현 태스크
- [x] 기존 팀이 있으면 TUI가 team picker list를 기본 화면으로 열도록 조정한다.
- [x] team picker 각 항목에 `result state`, `approvals`, `workers`, `tasks`, `attention reason`을 표시한다.
- [x] create mode에서도 `Esc`로 팀 목록으로 돌아갈 수 있게 정리한다.

### 자체 테스트
- [x] `tests/team-tui/app.test.tsx`에 team picker overview / attention-first rendering 회귀 테스트를 추가한다.
- [x] `npm run typecheck`
- [x] `npm test` — 총 183 tests 통과

### 이슈 및 수정
- [x] create modal 상태에서 team list shortcut을 전부 켜면 `TextInput` 입력과 충돌할 수 있어, 일반 shortcut은 그대로 두고 `Esc`만 별도 입력 훅으로 처리했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 3. 문서 / smoke / regression sync

### 목표
- README / AGENT / smoke 문서가 새 multi-team picker 동작과 최신 테스트 수를 설명하도록 맞춘다.

### 구현 태스크
- [x] README / AGENT / NEXT_BACKLOG에 multi-team overview 진행 상태를 반영한다.
- [x] TUI / quickstart 문서에 team picker overview 확인 절차를 추가한다.
- [x] 관련 테스트 수와 다음 우선순위를 최신 상태로 동기화한다.

### 자체 테스트
- [x] 문서에 적힌 screen/state 이름이 실제 구현과 일치하는지 수동 점검한다.
- [x] Phase 2 직후 확인한 `npm run typecheck` / `npm test` 결과(`183 tests pass`)를 문서 수치에 반영한다.
- [x] backlog / workflow / handoff의 다음 우선순위가 서로 모순되지 않는지 확인한다.

### 이슈 및 수정
- [x] multi-team picker가 완료되면서 다음 우선순위가 `generated files / preview large-output UX polish`로 이동했기 때문에 backlog / workflow / handoff 추천 순서를 함께 갱신했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## 현재 결과 요약

- `listTeams()`는 이제 팀별 `approvals`, `workers`, `tasks`, `attentionReasons`, `resultState`를 제공한다.
- TUI team picker는 attention-needed 팀을 위로 올리고, 요약 카운트를 함께 보여준다.
- 현재 구현 후 로컬 검증: `npm run typecheck`, `npm test` 통과 / 총 `183 tests pass`

## 최종 결과 요약

- `tui`는 이제 team을 생략하면 multi-team picker / overview부터 시작할 수 있다.
- team picker row는 `resultState`, `approvals`, `workers`, `tasks`, `attention reason`을 함께 보여준다.
- README / AGENT / quickstart / TUI smoke / backlog / workflow 문서가 이 동작과 최신 테스트 수(`183`)에 맞게 동기화됐다.
