# implement_20260403_201710.md

작성 일시: `2026-04-03 20:17:10 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
team-tui를 compact/narrow responsive shell로 재구성해 작은 터미널에서도 읽기 쉽게 만든다.

## 개발 범위
control/watch TUI의 responsive shell, compact 2패널, narrow 단일패널, 하단 detail tab 구조 도입에 한정한다.

## 제외 범위
- 새 runtime/backend 추가
- project preset/role 체계 변경
- 원격 UI/웹앱 추가

## 참조 문서
- README.md
- AGENT.md
- docs/DEV_PLAN_WORKFLOW.md
- docs/TUI_SMOKE.md

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약
- [x] Phase 1. Responsive shell foundation 완료
- [x] Phase 2. Compact detail tabs 완료
- [x] Phase 3. Scroll and focus polish 완료

---

## Phase 1. Responsive shell foundation

### 목표
- control/watch TUI가 터미널 폭에 따라 `wide / compact / narrow` 셸로 반응하도록 만들고,
  작은 터미널에서는 한 화면에 모든 패널을 동시에 쌓지 않도록 기본 구조를 바꾼다.

### 구현 태스크
- [x] viewport 기준 layout mode 계산 유틸을 추가한다.
- [x] `TeamTuiApp`에 `wide / compact / narrow` responsive shell 분기를 추가한다.
- [x] narrow 모드에서 primary pane(`Tasks` / `Teammates`)와 detail pane(`Activity` / `Transcript`)를 분리한다.
- [x] detail tab 전환 단축키(`[`, `]`, 좌우 화살표`)와 도움말을 반영한다.
- [x] pane 컴포넌트가 shell 모드에 맞춰 가변 width / minHeight를 받을 수 있게 정리한다.

### 자체 테스트
- [x] narrow viewport에서 compact shell이 렌더되는지 TUI 테스트를 추가한다.
- [x] narrow mode에서 detail tab 전환이 동작하는지 TUI 테스트를 추가한다.
- [x] `npm run typecheck`를 통과한다.
- [x] `npm run build && node --test dist/tests/team-tui/app.test.js`를 통과한다.

### 이슈 및 수정
- [x] `ink-testing-library`의 `render()`는 두 번째 인자로 render options를 받지 않아, viewport는 `TeamTuiApp` prop 주입 방식으로 테스트했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 2. Compact detail tabs

### 목표
- `ProjectStudioApp`의 긴 우측 패널 스택을 `Status + Tasks + detail tab` 구조로 접어,
  compact/narrow 터미널에서도 generated files / preview / teammates를 번갈아 볼 수 있게 만든다.

### 구현 태스크
- [x] `ProjectStudioApp`에 viewport 기반 `wide / compact / narrow` layout mode를 연결한다.
- [x] 우측 `Generated Files / Output Preview / Teammates` 패널을 단일 detail 영역으로 접는다.
- [x] detail tab(`Files / Preview / Teammates`) 상태와 키 입력(`[`, `]`)을 추가한다.
- [x] wide 모드와 compact/narrow 모드에서 같은 detail tab 구조를 재사용하도록 정리한다.
- [x] 기존 완료/실행중 상태 테스트를 새 기본 탭 동작에 맞게 갱신한다.

### 자체 테스트
- [x] compact viewport에서 `Generated Files`가 기본 detail panel로 렌더되는지 TUI 테스트를 추가한다.
- [x] 키 입력으로 `Files -> Preview -> Teammates` 전환이 되는지 TUI 테스트를 추가한다.
- [x] 기존 preview / executing-turn 테스트를 새 tabbed UI 기준으로 업데이트한다.
- [x] `npm run typecheck`를 통과한다.
- [x] `npm run build && node --test dist/tests/team-tui/project-builder-app.test.js`를 통과한다.
- [x] `npm test`를 통과한다.

### 이슈 및 수정
- [x] 기본 detail tab이 `Files`로 바뀌면서 preview/teammate 관련 기존 테스트가 실패해,
      새 UX 기준(`Files -> Preview -> Teammates`)으로 기대값과 대기 조건을 조정했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 3. Scroll and focus polish

### 목표
- compact/narrow TUI에서 긴 리스트를 실제로 읽을 수 있도록 pane windowing, detail scroll,
  `focus mode`를 추가해 작은 터미널에서도 작업 상태를 따라가기 쉽게 만든다.

### 구현 태스크
- [x] `Tasks` / `Teammates` pane에 선택 기반 windowing과 `showing x-y of n` 요약을 추가한다.
- [x] `Activity` / `Transcript` pane에 detail scroll window와 `showing x-y of n` 요약을 추가한다.
- [x] `TeamTuiApp`에 `focus mode(none -> primary -> detail)`를 추가한다.
- [x] 상태바와 도움말에 `f`, `j/k` 단축키를 반영한다.
- [x] detail scroll을 위해 dashboard transcript/activity fetch limit을 확장한다.

### 자체 테스트
- [x] focus mode가 `primary -> detail`로 순환하며 한 pane만 확장하는지 TUI 테스트를 추가한다.
- [x] detail focus에서 transcript가 `j/k`로 스크롤되는지 TUI 테스트를 추가한다.
- [x] `npm run typecheck`를 통과한다.
- [x] `npm run build && node --test dist/tests/team-tui/app.test.js`를 통과한다.
- [x] `npm test`를 통과한다.

### 이슈 및 수정
- [x] 초기 구현에서는 dashboard transcript/activity fetch limit이 8로 고정돼
      detail scroll이 충분한 데이터를 받지 못했다. fetch limit을 24로 올려 해결했다.
- [x] transcript scroll 테스트의 `Transcript entry 1` 정규식이 `entry 10`까지 잡는 문제가 있어
      단어 경계 정규식으로 수정했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 이번 plan 범위 완료
