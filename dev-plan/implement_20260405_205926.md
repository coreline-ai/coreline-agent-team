# implement_20260405_205926.md

작성 일시: `2026-04-05 20:59:26 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
longer burn-in 결과를 release 후보별로 더 오래 축적하고 판독 가능하게 만든다.

## 개발 범위
Codex repeated soak artifact history 보존, release-candidate labeling, 장시간 burn-in 운영 기준 고정에 한정한다.

## 제외 범위
- 새 runtime/backend 추가
- direct API 기반 LLM 경로 추가
- multi-team / global dashboard 재설계
- direct upstream parity 구현

## 참조 문서
- 이전 개발 계획: dev-plan/implement_20260405_203529.md
- README.md
- AGENT.md
- docs/NEXT_BACKLOG.md
- docs/RELEASE_CHECKLIST.md
- docs/CODEX_REPEATED_SOAK.md
- docs/RELIABILITY_CHECKLIST.md
- docs/DEV_PLAN_WORKFLOW.md

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약
- [x] Phase 1. artifact history / release-candidate baseline 완료
- [x] Phase 2. burn-in runner / checker history surface 완료
- [x] Phase 3. 문서 / 운영 절차 sync 완료

---

## Phase 1. artifact history / release-candidate baseline

### 목표
- repeated soak 결과를 `latest-summary.json` 한 장으로 덮어쓰지 않고,
  release 후보(label)와 함께 누적 보관할 수 있는 artifact baseline을 고정한다.

### 구현 태스크
- [x] `CodexRepeatedSoakOptions` / summary / result 타입에 `runLabel`, archived summary path, history manifest path를 추가한다.
- [x] `latest-summary.json` 외에 timestamped `summary-*.json` archive를 함께 기록한다.
- [x] `history.json` manifest를 추가해 labeled burn-in 결과를 누적 보관한다.
- [x] `scripts/run-codex-repeated-soak.mjs`에 `--label` surface를 추가한다.
- [x] success / failure / multi-run append 시나리오 테스트를 보강한다.

### 자체 테스트
- [x] `tests/team-cli/codex-repeated-soak.test.ts`에 summary archive / history manifest / label append 검증을 추가한다.
- [x] `npm run typecheck`
- [x] `npm test`
- [x] 결과 확인: **189 pass / 0 fail**

### 이슈 및 수정
- [x] `buildSummaryArtifact()`의 `Pick<CodexRepeatedSoakResult, ...>`에 `runLabel`이 빠져 typecheck가 실패했고, 타입 범위에 `runLabel`을 추가해 수정했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 2. burn-in runner / checker history surface

### 목표
- archived summary / history manifest를 CLI runner와 gate checker에서 바로 읽기 쉽게 노출한다.

### 구현 태스크
- [x] `renderCodexRepeatedSoakSummary()`에 `label`, `summary_archive`, `history`, `history_runs` surface를 추가했다.
- [x] `scripts/check-codex-repeated-soak.mjs`에 `--history`, `--run-label`을 추가해 archived labeled run을 직접 판독할 수 있게 했다.
- [x] `resolveCodexRepeatedSoakSummarySelection()` / `readCodexRepeatedSoakHistoryManifest()` helper를 추가해 latest summary와 history manifest를 공통 방식으로 해석하도록 정리했다.
- [x] gate evaluation 출력에 `summary`, `history`, `history_runs`, `label` context를 포함했다.

### 자체 테스트
- [x] `tests/team-cli/codex-repeated-soak.test.ts`에 summary render / history selection / checker `--history --run-label` 회귀 테스트를 추가했다.
- [x] `npm run typecheck`
- [x] `npm test`
- [x] 결과 확인: **191 pass / 0 fail**

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 3. 문서 / 운영 절차 sync

### 목표
- longer burn-in 결과 축적 규칙과 release 후보 label 규칙을 문서에 고정한다.

### 구현 태스크
- [x] `docs/CODEX_REPEATED_SOAK.md`에 `summary-*.json`, `history.json`, `--label`, `--history`, `--run-label` 사용법을 반영했다.
- [x] `docs/RELEASE_CHECKLIST.md`와 `README.md`에 release 후보 artifact 보존 규칙과 labeled checker 예시를 반영했다.
- [x] `AGENT.md`, `docs/NEXT_BACKLOG.md`, `docs/DEV_PLAN_WORKFLOW.md`, `CLAUDE.md`를 최신 상태로 동기화했다.

### 자체 테스트
- [x] 문서 명령/경로/필드명 수동 대조
- [x] 코드 재실행은 생략하고, Phase 2 직후 녹색 기준(`npm run typecheck`, `npm test`, `191 pass`)을 문서와 동기화했다.
- [x] 결과를 확인했다.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능
