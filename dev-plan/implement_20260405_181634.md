# implement_20260405_181634.md

작성 일시: `2026-04-05 18:16:34 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
approval / permission preset UX를 안전하게 개선한다.

## 개발 범위
CLI permissions surface, approval inbox, persisted rule 결정 흐름 정리에 한정한다.

## 제외 범위
- 새 runtime/backend 추가
- direct API 기반 LLM 경로 추가
- multi-team overview 전면 재설계
- remote transport / pane backend 구현

## 참조 문서
- 이전 개발 계획: dev-plan/implement_20260405_171538.md
- README.md
- AGENT.md
- docs/TEAM_CONSTRAINTS.md
- docs/RELIABILITY_CHECKLIST.md
- docs/NEXT_BACKLOG.md

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- Codex CLI 중심 runtime 원칙과 기존 permission persistence 계약을 깨지 않는다.

## Phase 상태 요약
- [x] Phase 1. approval context surface 정리 완료
- [x] Phase 2. permission preset 결정 UX 완료
- [x] Phase 3. 문서/smoke/test 동기화 완료

---

## Phase 1. approval context surface 정리

### 목표
- pending/resolved permission request가 어떤 `cmd/cwd/path/host`를 요청하는지
  CLI와 TUI 양쪽에서 바로 읽히도록 정리한다.
- persisted rule 저장 전에 참고할 수 있는 suggested matcher 힌트를 같은 규칙으로 보여준다.

### 구현 태스크
- [x] permission request input에서 `cmd/cwd/path/host`를 추출하는 공통 helper를 만든다.
- [x] `permissions pending|resolved` 출력에 request context와 suggested matcher를 포함한다.
- [x] TUI approval modal에 selected request detail / suggested persistence 블록을 추가한다.
- [x] CLI/TUI 테스트를 추가하거나 갱신한다.

### 자체 테스트
- [x] `npm run typecheck`
- [x] `npm test`
- [x] `tests/team-cli/permission-commands.test.ts`
- [x] `tests/team-tui/app.test.tsx`

### 이슈 및 수정
- [x] approval context는 CLI와 TUI가 서로 다른 문자열을 만들지 않도록 `team-core/permission-context.ts` 공통 helper로 정리했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 2. permission preset 결정 UX

### 목표
- 자유입력 rule 저장만으로는 반복 운영이 번거로운 부분을 줄이고,
  common allow/deny matcher preset을 더 빠르게 적용할 수 있게 한다.

### 구현 태스크
- [x] persist decision에 자주 쓰는 matcher preset 후보(`suggested`, `command`, `cwd`, `path`, `host`)를 정한다.
- [x] CLI decision command에서 `--preset <...>` surface를 추가한다.
- [x] TUI approval inbox에서 preset 기반 저장 흐름과 available preset 표시를 추가한다.
- [x] 관련 테스트를 추가한다.

### 자체 테스트
- [x] `npm run typecheck`
- [x] `npm test` — 총 179 tests 통과
- [x] permission command / approval modal regression test

### 이슈 및 수정
- [x] `--preset`과 explicit `--match-*`를 함께 받으면 어떤 규칙이 우선인지 혼란스러우므로, parser 단계에서 조합을 금지했다.
- [x] TUI는 preset hotkey를 제공하지만, 회귀 테스트는 우선 “available preset surface 표시”와 CLI preset persistence를 중심으로 고정했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 3. 문서/smoke/test 동기화

### 목표
- README / AGENT / smoke 문서가 approval / permission UX와 같은 모델을 설명하도록 맞춘다.

### 구현 태스크
- [x] README에 permission / approval 운영 흐름과 관련 명령을 갱신한다.
- [x] CLI/TUI smoke 문서에 approval / permission 확인 절차를 반영한다.
- [x] AGENT / backlog 문서를 최종 상태에 맞게 동기화한다.

### 자체 테스트
- [x] `npm run typecheck`
- [x] `npm test` — 총 179 tests 통과
- [x] 문서에 적힌 명령과 실제 surface가 일치하는지 수동 점검한다.

### 이슈 및 수정
- [x] phase 3 동기화 중 테스트 수가 `176 → 179`로 증가했기 때문에 README / AGENT / CLAUDE 수치를 함께 갱신했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## 최종 결과 요약

- approval request는 이제 CLI/TUI에서 같은 `cmd/cwd/path/host` context로 읽힌다.
- persisted rule 저장은 matcher preset(`suggested/command/cwd/path/host`) 기반으로 빠르게 적용 가능하다.
- 관련 문서와 smoke 절차는 최신 surface에 맞게 동기화됐다.
- 최종 로컬 검증: `npm run typecheck`, `npm test` 통과 / 총 `179 tests pass`
