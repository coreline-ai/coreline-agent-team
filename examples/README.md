# examples

This directory is reserved for local demos once `team-runtime` can launch real
teammates.

Planned examples:

- create a team and task list
- send teammate mailbox messages
- spawn a local in-process teammate
